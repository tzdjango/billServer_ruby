<%= form_for(@call_charge) do |f| %>
  <% if @call_charge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@call_charge.errors.count, "error") %> prohibited this call_charge from being saved:</h2>

      <ul>
      <% @call_charge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </div>
  <div class="field">
    <%= f.label :bill_id %><br>
    <%= f.number_field :bill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
