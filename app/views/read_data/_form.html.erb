<%= form_for(@read_datum) do |f| %>
  <% if @read_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@read_datum.errors.count, "error") %> prohibited this read_datum from being saved:</h2>

      <ul>
      <% @read_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
