<p id="notice"><%= notice %></p>

<p>
  <strong>Total:</strong>
  <%= @package.total %>
</p>

<p>
  <strong>Bill:</strong>
  <%= @package.bill_id %>
</p>

<%= link_to 'Edit', edit_package_path(@package) %> |
<%= link_to 'Back', packages_path %>
